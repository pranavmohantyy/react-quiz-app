const questions = require('./questions.json');\n\nconst DifficultyFilter = () => {\n    const [difficulty, setDifficulty] = React.useState('Easy');\n    let timer = 10;\n    if (difficulty === 'Medium') timer = 20;\n    else if (difficulty === 'Hard') timer = 30;\n\n    return (\n        <div>\n            <select onChange={(e) => setDifficulty(e.target.value)}>\n                <option value="Easy">Easy</option>\n                <option value="Medium">Medium</option>\n                <option value="Hard">Hard</option>\n            </select>\n            <Timer time={timer} />\n        </div>\n    );\n};\n\nconst Timer = ({ time }) => {\n    const [seconds, setSeconds] = React.useState(time);\n\n    React.useEffect(() => {\n        const countdown = setInterval(() => {\n            setSeconds((prev) => prev - 1);\n        }, 1000);\n        return () => clearInterval(countdown);\n    }, []);\n\n    return <div>{seconds} seconds left</div>;\n};\n\nReactDOM.render(<DifficultyFilter />, document.getElementById('root'));