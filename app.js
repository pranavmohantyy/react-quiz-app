const questions = require('./questions.json');\n\nconst DifficultyFilter = () => {\n    const [difficulty, setDifficulty] = React.useState('Easy');\n    let timer = 10;\n    if (difficulty === 'Medium') timer = 20;\n    else if (difficulty === 'Hard') timer = 30;\n\n    return (\n        <div>\n            <select onChange={(e) => setDifficulty(e.target.value)}>\n                <option value="Easy">Easy</option>\n                <option value="Medium">Medium</option>\n                <option value="Hard">Hard</option>\n            </select>\n            <Timer time={timer} />\n        </div>\n    );\n};\n\nconst ReviewMode = ({ userAnswers }) => {\n    return (\n        <div>\n            <h2>Review Your Answers</h2>\n            {questions.map((question, index) => (\n                <div key={index}>\n                    <p>{question.question}</p>\n                    <p>Your Answer: {question.options[userAnswers[index]]}</p>\n                    <p>Correct Answer: {question.options[question.correctAnswer]}</p>\n                </div>\n            ))}\n        </div>\n    );\n};\n\n// Add state to manage user answers and toggle review mode\nconst QuizApp = () => {\n    const [userAnswers, setUserAnswers] = React.useState([]);\n    const [isReviewMode, setIsReviewMode] = React.useState(false);\n\n    const handleAnswer = (answerIndex) => {\n        setUserAnswers([...userAnswers, answerIndex]);\n    };\n\n    const handleReview = () => {\n        setIsReviewMode(!isReviewMode);\n    };\n\n    return (\n        <div>\n            {isReviewMode ? (\n                <ReviewMode userAnswers={userAnswers} />\n            ) : (\n                <div>\n                    {/* Quiz Questions Rendering Here */} \n                    <button onClick={handleReview}>Review Answers</button>\n                </div>\n            )} \n        </div>\n    );\n};\n\nReactDOM.render(<QuizApp />, document.getElementById('root'));